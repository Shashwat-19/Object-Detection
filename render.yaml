services:
  - type: web
    name: object-detection-tracker
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
